// ============================================================
// Website Builder Pro — Editor Page (Server Component Entry)
// ============================================================

import BuilderClient from '../BuilderClient';

export default function EditorPage() {
  return <BuilderClient />;
}
